import React, { Component } from 'react';
import './${name}.scss';
class ${name^} extends Component {
    render() {
        return (
            <div className="${name}"></div>
        );
    }
}

export default ${name^};